{
  "functions": [
    {
      "source": "functions",
      "codebase": "default",
      "ignore": [
        "node_modules",
        ".git",
        "firebase-debug.log",
        "firebase-debug.*.log"
      ],
      "predeploy": [
        "cd functions && npm run build && cd .. && rm -rf dist && npm run generate && rm -rf functions/.nuxt && mkdir -p functions/.nuxt/dist/ && cp -r .nuxt/dist/server functions/.nuxt/dist/ && cp nuxt.config.ts functions/"
      ]
    }
  ],
  "hosting": {
    "predeploy": [
      "rm -rf public/* && mkdir -p public/_nuxt && cp -r .nuxt/dist/client/ public/_nuxt && cp static/robots.txt public/ && cp dist/sitemap.xml public/ && cp dist/sitemap.xml.gz public/"
    ],
    "public": "public",
    "ignore": [
      "firebase.json",
      "**/.*",
      "**/node_modules/**"
    ],
    "rewrites": [
      {
        "source": "**",
        "function": "ssrapp"
      }
    ]
  }
}
